// Store the names of a few friends in an array
var names = ["Ayesha", "Fahad", "iqra", "Noor"];
// Greet each person in the array by printing a personalized message to the console
for (var i = 0; i < names.length; i++) {
    console.log("Hello Mr, ".concat(names[i], "!"));
}
